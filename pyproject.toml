[project]
name = "rag"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "alexxanton", email = "alexantonaranda@gmail.com" }
]
requires-python = ">=3.15"
dependencies = [
    "flake8>=7.3.0",
    "mypy>=2.3.1",
]

[tool.uv]
package = false

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
